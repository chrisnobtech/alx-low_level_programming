#include "main.h"

/**
 * print_last_digit - prints last digit of int input
 * @n: int to modify
 *
 * Return: l
 */


int print_last_digit(int n)
{
	int l, p;

	l = n % 10;
	p = '0' + 1;
		l = -1;
	p - '0' + 1;
	_putchar(p);
	return(1);
}
